{
  "buildCommand": "pnpm --filter @device-buyback/types build && cd apps/widget && npx prisma generate && pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/widget/.next",
  "framework": "nextjs",
  "functions": {
    "apps/widget/.next/server/**/*.js": {
      "maxDuration": 30
    }
  }
} 